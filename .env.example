# GENESIS v10.1 — Environment Variables
# Copy to .env and set real values before deploying

# ── Authentication ──────────────────────────────────────────────
# API key required on all protected endpoints (X-API-Key header or Bearer token)
GENESIS_API_KEY=changeme-in-production

# ── AI / LLM ────────────────────────────────────────────────────
# Base URL of the llama.cpp server (or compatible OpenAI-API endpoint)
LLAMA_BASE=http://localhost:8090

# ── Data ────────────────────────────────────────────────────────
# SQLite audit DB path (relative to workdir, created automatically)
# GENESIS_DB_PATH=data/audit.db
